pre-commit:
  parallel: true
  jobs:
    - name: biome
      glob: '*.{js,mjs,ts,json,jsonc,css}'
      run: pnpm exec biome check --staged --no-errors-on-unmatched
    - name: astro-check
      glob: '*.{astro,ts,mjs}'
      run: pnpm exec astro check
